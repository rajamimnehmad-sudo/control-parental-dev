# Authorized static local-raster transport

User explicitly authorized the architecture and necessary Chrome/Photos work after454 triage. Same06C owner/worktree/anchor76f285; current governance9323b126. Central resumed in_progress at31c0358b. GIF/video/model/thresholds/authority changes excluded.

First candidate: asynchronous same-origin POST of static data: JPEG/PNG/WebP/AVIF, exact claimed SELF_READY capability validated before/after inference; existing image content authority, decision session and final visual Byte Gate reused. Opaque192-bit result handles retain only gate-authorized SAFE originals or existing BLOCK/UNKNOWN replacements. Every asset GET revalidates owner/session and Byte Gate. The already routed internal fixture origin serves only the reserved asset prefix for this transport; no website-specific routing or bypass. CSP adds that exact image path, while the submission channel remains self. Existing document release protocol is unchanged and remains synchronous.

Bounds: request1.4MiB enforced before body read for origin and absolute HTTP forms, decoded image1MiB,2 admitted submissions/5s bounded admission wait, existing2 image bodies/1 inference/2 decision queue,128 assets/8MiB total. Result arrays are immutable and clearing the cache does not mutate an in-progress response. Native client queue128 jobs/8MiB source text, at most1 active request per document,15s request deadline; initial subset accepts base64 static raster URLs. Unsupported schemes/formats remain closed. No unclassified input is stored as a deliverable asset. CORS, only when requested, names the exact submitting origin, never wildcard; original crossorigin attributes are not changed.

Client synchronous mutation guards remove local image sources before submitting; global data/blob CSP and immutable shield remain. Pending sources are inert for clone/picture handling. Source replacement/removal invalidates the job; pagehide aborts active XHR and discards queued work. An already-running bounded native inference may finish, but a late client callback cannot apply it to a replaced source or departed document. No new main-thread synchronous photo analysis. Temporary job payload references are released after completion.

Validation planned: fake SAFE/raw UNKNOWN Byte Gate rejection, safe identity/block/unknown, invalidated sessions, origin/CORS isolation, bounded eviction and close, malformed/mislabeled/oversized input, HTTP body limits; physical static/dynamic/srcset/picture/clone/Shadow/removal/replacement and exact byte comparison. Then real Google/Mimo/Fravega, latency, lifecycle and final>=45min health. Existing parser/timer test now excludes only the explicit photo-scheduling fragment; it still forbids timers in the document authority code.

Primary references reviewed: [WHATWG XMLHttpRequest abort](https://xhr.spec.whatwg.org/#the-abort()-method), [Fetch data URLs](https://fetch.spec.whatwg.org/#data-urls), [CSP3](https://www.w3.org/TR/CSP3/). Cancellation is not claimed to preempt already-running native inference. No dependencies, remote compute, costs or cloud services introduced.
